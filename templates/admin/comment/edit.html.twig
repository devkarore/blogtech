{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}
    <h1>Edit Comment</h1>

        {{ include('admin/comment/_form.html.twig', {'button_label': 'Update'}) }}

        <button type="button" data-controller="comment-status" data-action="click->comment-status#toggle" data-comment-status-url-value="{{ path('toggle_status', {'id': comment.id}) }}" >
            Changer le statut
        </button>

        <a href="{{ path('app_admin_comment_index') }}">back to list</a>

        {{ include('admin/comment/_delete_form.html.twig') }}

{% endblock %}
